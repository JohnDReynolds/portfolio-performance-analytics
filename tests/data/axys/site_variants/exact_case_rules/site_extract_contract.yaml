version: 1
datasets:
  transactions.csv:
    columns:
      SEC_TYPE:
        requires_context_for_semantics: true
        blocking_if_missing: true
      SRC_DEST_SYMBOL:
        requires_context_for_semantics: true
        blocking_if_missing: true
